= render partial: "projects/menu", as: :project, object: @project
hr/

- url = project_slack_channel_path(@project, @slack_channel, redirect_to: params[:redirect_to])
= simple_form_for @slack_channel, method: :put, url: url do |f|
  = render partial: "form", as: :f, object: f
