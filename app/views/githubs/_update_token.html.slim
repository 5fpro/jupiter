= link_to "Update Token Permisssion", auth_github_path(scope: "user,user:email,repo,admin:repo_hook", origin: new_project_github_url(@project)), class: "btn btn-danger" if current_user.token_need_update?
