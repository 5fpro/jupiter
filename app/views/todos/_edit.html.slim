= simple_form_for todo, as: :todo, method: :put, remote: true, url: todo_path(todo, format: :js) do |f|
  = f.input_field :desc, as: :text
  = f.submit
  = link_to "取消", "#", "data-close-form" => true
