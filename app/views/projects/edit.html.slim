= render partial: "menu", as: :project, object: @project

= simple_form_for @project, url: project_path(@project), method: :put, wrapper: :horizontal_form do |f|
  = render partial: "form", object: f, as: :f
