= render partial: "menu", as: :project, object: @project

= simple_form_for @project, url: project_path(@project), method: :put do |f|
  = f.input :description, as: :text, label: "Description"
  = f.submit "Update"
