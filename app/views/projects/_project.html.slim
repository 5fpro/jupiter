h2= link_to project.name, project_path(project)
= render partial: "projects/record_stats", as: :records, object: project.records.merge(records)
= link_to "記錄時間", new_project_record_path(project, format: :js), remote: true
.form-new-record
